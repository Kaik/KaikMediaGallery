{% block _settings_hooks_providers_entry_modules_entry_areas_entry_entry_display_widget %}
    {% spaceless %}
        <div class="box box-default">
            <div class="box-header with-border">
                <h4 class="box-title">{{ __('Display') }}</h4>
                <div class="row text-uppercase small" style="margin-left: -10px; margin-right:-10px;">
                    <div class="col-md-2" >
                        {{ __('Internal name') }}
                    </div>
                    <div class="col-md-2">
                        {{ __('Css') }}
                    </div>
                    <div class="col-md-1">
                        {{ __('Width') }}
                    </div>
                    <div class="col-md-1">
                        {{ __('Height') }}
                    </div>
                    <div class="col-md-2">
                        {{ __('Mode') }}
                    </div>
                    <div class="col-md-2">
                        {{ __('Show title') }}
                    </div>
                    <div class="col-md-2">
                        {{ __('Show Legal') }}
                    </div>
                </div>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
                {{ include('@KaikmediaGalleryModule/Form/display.prototype.html.twig', { 'form': form }) }}
            </div>
            <div class="box-footer">
            </div>
            <!-- /.box-body -->
        </div>
<!-- /.box -->
    {% endspaceless %}
{% endblock %}