				{# Feature user gallery #}
				<div class="col-md-12 {% if settings.KaikmediaGalleryModule.user.enabled == '0' and modulename != 'KaikmediaGalleryModule' %}hide {% endif %}" style="margin-bottom:20px;">						
				  <div class="row">
					<div class="col-md-12 text-right">				
						<span>{{ __('User uploads gallery')}}</span>
						{# Feature user on/off#}	
						<div class="btn-group btn-xs" data-toggle="buttons">
			                  <label class="btn btn-xs {% if modulesettings.user.enabled == '0' %}active btn-primary {% else %} btn-primary {% endif %}">
			                    <input type="radio" name="{{ modulename }}_user_enabled" id="option1" autocomplete="off" value="0" {% if modulesettings.user.enabled == '0' %}checked {% endif %}> {{ __('Off') }}
			                  </label>
			                  <label class="btn btn-xs {% if modulesettings.user.enabled == '1' %}active btn-primary {% else %} btn-primary {% endif %}">
			                    <input type="radio" name="{{ modulename }}_user_enabled" id="option2" autocomplete="off" value="1" {% if modulesettings.user.enabled == '1' %}checked {% endif %}> {{ __('On') }}
			                  </label>
							 	<button class="btn btn-xs" data-toggle="collapse" data-target="#{{ modulename }}_feature_user" {% if settings.KaikmediaGalleryModule.user.enabled == '0' or modulesettings.user.enabled == '0' %}disabled="disabled" {% endif %} ><i class="fa fa-cog"></i></button>				                  			                  
			            </div>													
					</div>	
												
					{# Feature user settings#}	
					<div id="{{ modulename }}_feature_user" class="col-md-12 collapse">				
					<div class="row bg-info">
						<div class="col-md-12" style="padding-top:5px;">																		
				           				            	
				            <div class="btn-xs form-group col-md-2 pull-right">
					            <label > {{ __('Per page') }} 	
				                <input type="text" class="form-control input-sm" name="{{ modulename }}_user_perpage" value="{% if modulesettings.user.perpage is defined %}{{ modulesettings.user.perpage }}{% endif %}">
				                </label>			                
				            </div>					            				
				            <div class="btn-xs form-group col-md-2 pull-right">	
					            <label > {{ __('Feature type') }} 	
				                <input type="text" class="form-control input-sm" name="{{ modulename }}_user_type" value="{% if modulesettings.user.type is defined %}{{ modulesettings.user.type }}{% endif %}">
				                </label>					                
				            </div>									            
	            		</div> 				
					  </div>	                  		                  		
            		</div> 		
					{# Feature user settings end #}	            		           				
				  </div>			  
				</div>				

				{# Feature user end #}				
